const contentOffset = 2;

export const sizes = {
  contentOffset: contentOffset,
  doubleOffset: 2 * contentOffset,
  halfOffset: 0.5 * contentOffset,
  navBarHeight: 8,
  cornerRadius: 0.8,
  componentHeight: 4,
};